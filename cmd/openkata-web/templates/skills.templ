package templates

import "fmt"

templ Skills(skills []SkillEntry) {
	@Layout("Skills") {
		@Nav()

		<main>
			<section class="hero hero--kanji">
				<span class="hero-kanji">技</span>
				<div class="hero-text">
					<h1 class="cursor">Skills</h1>
					<p class="comment">Portable, platform-agnostic practices for AI agents.</p>
				</div>
			</section>

			if len(skills) == 0 {
				<p>No skills available yet.</p>
			} else {
				<div class="artifact-list">
					for i, s := range skills {
						<details class="artifact-entry">
							<summary class="artifact-summary">
								<div class="artifact-summary-top">
									<span class="artifact-number">{ fmt.Sprintf("%02d", i+1) }</span>
									<span class="artifact-name">{ s.Name }</span>
									<span class="artifact-summary-right">
										<span class="artifact-downloads">{ fmt.Sprintf("%d", s.Downloads) } ↓</span>
										<span class="artifact-sep">|</span>
										<span class="artifact-version">v{ s.Version }</span>
										<span class="artifact-toggle-icon"></span>
									</span>
								</div>
								<div class="artifact-tags">
									if len(SplitTags(s.Tags)) > 0 {
										for _, tag := range SplitTags(s.Tags) {
											<span class={ TagClass(tag) }>{ tag }</span>
										}
									}
									<a class="btn-open" href={ templ.SafeURL(fmt.Sprintf("/skills/%s/", s.Name)) }>Open</a>
								</div>
							</summary>
							<div class="artifact-body">
																if len(FirstSentence(s.Description)) > 200 {
									<p class="artifact-description"><strong>{ FirstSentence(s.Description) }</strong></p>
								} else {
									<p class="artifact-description"><strong>{ FirstSentence(s.Description) }</strong><br/>{ AfterFirstSentence(s.Description) }</p>
								}
							</div>
						</details>
					}
				</div>
			}
		</main>

		<footer class="footer">
			© 2026 OPENKATA.dev // License: <a href="https://github.com/tovrleaf/openkata/blob/main/LICENSE" target="_blank" rel="noopener">MIT</a>
		</footer>
	}
}
