FROM ubuntu:24.04
RUN apt-get update && apt-get install -y \
    curl git bash jq unzip && \
    rm -rf /var/lib/apt/lists/*
RUN curl -fsSL https://cli.kiro.dev/install | bash
ENV PATH="/root/.local/bin:${PATH}"
WORKDIR /workspace
