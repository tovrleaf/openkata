{
  "name": "openkata/git-worktree",
  "version": "0.1.0",
  "private": true,
  "summary": "Manages parallel workspaces using git worktrees. Activate when the user needs to work on multiple branches simultaneously, run parallel tasks (evals, builds, reviews) in isolation, or when an agent needs its own workspace. Also activate when the user says \"worktree\", \"parallel branch\", \"work on two things at once\", or \"run these in parallel\".\n",
  "skills": {
    "git-worktree": {
      "path": "SKILL.md"
    }
  }
}
